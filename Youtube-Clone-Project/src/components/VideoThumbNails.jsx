import React from 'react';

const VideoThumbNails = () => {
    return (
        <div>
            <ul>
           {videos.map((video => 
            <li VideoShow key={id} /> ))}
            </ul> 
          
        </div>
    );
}

export default VideoThumbNails;
